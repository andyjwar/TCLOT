{
  "name": "tclot-push-api",
  "private": true,
  "description": "Cloudflare Worker — Web Push subscriptions + league alerts for TCLOT",
  "type": "module",
  "scripts": {
    "login": "wrangler login",
    "deploy": "wrangler deploy",
    "whoami": "wrangler whoami",
    "generate-vapid": "node scripts/generate-vapid-keys.mjs",
    "test": "node --test src/cron.test.js"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^1.0.2"
  },
  "devDependencies": {
    "web-push": "^3.6.7",
    "wrangler": "^3.91.0"
  }
}
