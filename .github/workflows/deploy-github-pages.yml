# Builds web/ and deploys to GitHub Pages.
# After pushing: Repo → Settings → Pages → Build: GitHub Actions (not "Deploy from branch").
name: Deploy site to Pages

on:
  push:
    branches: [main, master]
  workflow_dispatch:
  # Schedules (UTC):
  # - 30 5/13/21 * * * — three times daily: full data refresh (gate always allows via daily windows in script)
  # - 0 * * * *  — every hour: post-waivers window, post-deadline ingest window, pre-waivers 3-hourly
  #   window (24h before waivers_time), or the daily catch-all windows
  #   (see web/scripts/waiver-refresh-gate.mjs). Push / workflow_dispatch always deploy.
  # - */15 * * * * — burst cron: gate deploys ONLY inside ~90 min after each waivers_time
  #   (waivers land within minutes); skips immediately at all other times.
  schedule:
    - cron: '30 5 * * *'
    - cron: '30 13 * * *'
    - cron: '30 21 * * *'
    - cron: '0 * * * *'
    - cron: '*/15 * * * *'

permissions:
  contents: read
  pages: write
  id-token: write

# Latest run wins — avoids deploy-pages 400 when two runs hit Pages API at once
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    # Same environment as deploy so FPL_LEAGUE_ID works whether you put it under
    # Repository secrets OR Environment (github-pages) secrets.
    environment:
      name: github-pages
    outputs:
      should_deploy: ${{ steps.gate.outputs.proceed }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: web/package-lock.json

      # Pushes and manual runs always deploy. Scheduled: hourly is gated to post-waiver windows + daily slot;
      # the */15 burst cron (SCHEDULE_CRON) is gated to the ~90 min burst window after each waivers_time.
      - name: Waiver / daily schedule gate
        id: gate
        env:
          SCHEDULE_CRON: ${{ github.event.schedule }}
        run: |
          if [ "$GITHUB_EVENT_NAME" != "schedule" ]; then
            echo "proceed=1" >> "$GITHUB_OUTPUT"
            echo "Not a cron — full deploy"
            exit 0
          fi
          if node web/scripts/waiver-refresh-gate.mjs; then
            echo "proceed=1" >> "$GITHUB_OUTPUT"
          else
            echo "proceed=0" >> "$GITHUB_OUTPUT"
            echo "Skipped — not in post-waivers or daily catch-up window (see web/scripts/waiver-refresh-gate.mjs)"
          fi

      # Without this, GH Pages bundles `fplApiBase()` → direct FPL (CORS) and Live/Draft APIs break.
      - name: Require FPL proxy URL for Pages build
        if: steps.gate.outputs.proceed == '1'
        env:
          VITE_FPL_PROXY_URL: ${{ secrets.VITE_FPL_PROXY_URL || vars.VITE_FPL_PROXY_URL }}
        run: |
          if [ "${{ vars.FPL_PROXY_OPTIONAL }}" = "true" ]; then
            echo "FPL_PROXY_OPTIONAL=true — building without Worker URL (Live tab will not work on Pages)"
            exit 0
          fi
          if [ -z "${VITE_FPL_PROXY_URL:-}" ]; then
            echo "::error::VITE_FPL_PROXY_URL is not set — GitHub Pages cannot call FPL from the browser (CORS). Add the Cloudflare Worker base URL under Settings → Secrets and variables → Actions (or github-pages Environment) as repository secret **VITE_FPL_PROXY_URL** (no trailing slash). See DEPLOY.md § Live tab proxy. Forks/demo only: create repository Variable **FPL_PROXY_OPTIONAL** = true."
            exit 1
          fi
          # A set-but-malformed value bakes a broken URL into the bundle: the browser's
          # fetch() then throws (Safari: "URL is not valid or contains user credentials.")
          # and the Live tab dies at runtime while CI stays green. Fail here instead.
          if ! node -e "
            const raw = (process.env.VITE_FPL_PROXY_URL ?? '').trim();
            let u;
            try { u = new URL(raw); } catch {
              console.error('not parseable as a URL');
              process.exit(1);
            }
            if (u.protocol !== 'https:') { console.error('protocol is ' + u.protocol + ' — must be https:'); process.exit(1); }
            if (u.username || u.password) { console.error('contains user credentials — browsers refuse to fetch such URLs'); process.exit(1); }
            if (/\s/.test(raw)) { console.error('contains whitespace'); process.exit(1); }
          "; then
            echo "::error::VITE_FPL_PROXY_URL is set but is not a valid https URL. The value must be the bare Worker base URL, e.g. https://your-worker.workers.dev — no quotes, no NAME= prefix, no embedded credentials, no stray whitespace/newline. Check BOTH Settings → Secrets and variables → Actions AND Settings → Environments → github-pages (environment secrets silently override repository secrets)."
            exit 1
          fi
          echo "VITE_FPL_PROXY_URL configured and valid — baked into this build for FPL Live + draft picks in browser."

      # League id: committed `league-id` file is source of truth. The github-pages
      # Environment secret FPL_LEAGUE_ID silently overrides the repository secret
      # of the same name — a stale 6802 there is what ingested a stranger's league
      # in 26/27. Secrets/vars are a fallback only when the file is absent.
      - name: Fetch real league from FPL API
        if: steps.gate.outputs.proceed == '1'
        run: |
          set -euo pipefail
          FILE_ID=""
          if [ -f league-id ]; then FILE_ID="$(tr -d '[:space:]' < league-id)"; fi
          SECRET_ID="${{ secrets.FPL_LEAGUE_ID }}"
          VAR_ID="${{ vars.FPL_LEAGUE_ID }}"
          ID="$FILE_ID"
          if [ -z "$ID" ]; then ID="$SECRET_ID"; fi
          if [ -z "$ID" ]; then ID="$VAR_ID"; fi
          if [ -n "$FILE_ID" ] && [ -n "$SECRET_ID" ] && [ "$SECRET_ID" != "$FILE_ID" ]; then
            echo "::warning::github-pages/repo FPL_LEAGUE_ID secret differs from committed league-id ($FILE_ID). Using the committed file. Environment secrets override repository secrets — update Settings → Environments → github-pages as well as repository secrets. Names are case-sensitive (FPL_LEAGUE_ID, not FPL_League_ID)."
          fi
          if [ -n "$ID" ]; then
            echo "Ingest: fetching FPL Draft league $ID"
            python3 -m pip install --user requests
            if LEAGUE_ID="$ID" python3 ingest.py; then
              INGESTED="$(python3 -c "import json; print(json.load(open('data/details.json'))['league']['id'])")"
              echo "Ingest OK — league id $INGESTED ($(wc -c < data/details.json) bytes)"
              if [ -n "$FILE_ID" ] && [ "$INGESTED" != "$FILE_ID" ]; then
                echo "::error::Ingested league id $INGESTED does not match committed league-id $FILE_ID — deploying committed web/public/league-data/ instead."
                rm -rf data
              fi
            else
              # FPL API down (e.g. seasonal "Game Updating" page) or league ID invalid.
              # Drop partial data/ so copy-data.js falls back to committed web/public/league-data/.
              echo "::warning::FPL ingest failed — deploying committed web/public/league-data/ instead of fresh data (FPL API may be down, e.g. season rollover)."
              rm -rf data
            fi
          else
            echo "No league-id file or FPL_LEAGUE_ID — build will use committed web/public/league-data/ or demo."
            echo "Add a committed league-id file, or secret: Settings → Secrets → Actions → FPL_LEAGUE_ID"
          fi

      # Relative base (./) so JS + league-data load even if URL casing/path differs
      - name: Install & build
        if: steps.gate.outputs.proceed == '1'
        working-directory: web
        env:
          VITE_BASE_PATH: "./"
          # Fallback only; readLeagueId prefers the committed league-id file.
          FPL_LEAGUE_ID: ${{ secrets.FPL_LEAGUE_ID || vars.FPL_LEAGUE_ID }}
          LEAGUE_ID: ${{ secrets.FPL_LEAGUE_ID || vars.FPL_LEAGUE_ID }}
          # Repository secret/variable, or add the same name under Settings → Environments → github-pages
          VITE_FPL_PROXY_URL: ${{ secrets.VITE_FPL_PROXY_URL || vars.VITE_FPL_PROXY_URL }}
          VITE_PUSH_API_URL: ${{ secrets.VITE_PUSH_API_URL || vars.VITE_PUSH_API_URL }}
          VITE_VAPID_PUBLIC_KEY: ${{ secrets.VITE_VAPID_PUBLIC_KEY || vars.VITE_VAPID_PUBLIC_KEY }}
          VITE_LEAGUE_DATA_REVISION: gw-${{ github.sha }}
        run: |
          echo "VITE_BASE_PATH=$VITE_BASE_PATH (relative — use URL with trailing slash: …/repo-name/)"
          if [ -n "$VITE_FPL_PROXY_URL" ]; then
            echo "VITE_FPL_PROXY_URL is set for Vite (Live tab + draft API proxy)"
          else
            echo "VITE_FPL_PROXY_URL omitted (allowed only when FPL_PROXY_OPTIONAL=true)"
          fi
          if [ -n "$VITE_PUSH_API_URL" ] && [ -n "$VITE_VAPID_PUBLIC_KEY" ]; then
            echo "Web push env is set — Settings push toggle will be enabled"
          else
            echo "Web push env omitted — Settings shows push as not configured"
          fi
          if [ -f ../league-id ]; then
            export FPL_LEAGUE_ID="$(tr -d '[:space:]' < ../league-id)"
            export LEAGUE_ID="$FPL_LEAGUE_ID"
            echo "Using committed league-id $FPL_LEAGUE_ID for draft picks / wire stats"
          elif [ -z "${FPL_LEAGUE_ID:-}" ]; then
            echo "::notice::No league-id file or FPL_LEAGUE_ID — build-draft-picks skipped; committed draft_picks.json is copied to dist."
          else
            echo "FPL_LEAGUE_ID is set — build-draft-picks will run during npm run build"
          fi
          npm ci
          npm run build
      - name: Verify league data shipped
        if: steps.gate.outputs.proceed == '1'
        run: |
          test -f web/dist/league-data/details.json || { echo "ERROR: no details.json in dist"; exit 1; }
          test -s web/dist/league-data/details.json || { echo "ERROR: empty details.json"; exit 1; }
          echo "OK — league-data in dist ($(wc -c < web/dist/league-data/details.json) bytes)"
          if grep -q '"isSample".*true' web/dist/league-data/details.json 2>/dev/null; then
            echo "::warning::Site is still DEMO sample data. Set FPL_LEAGUE_ID (secret or variable) or run publish-real-league and commit league-data."
          fi
          PNGS=$(find web/dist/team-logos -maxdepth 1 -name '*.png' 2>/dev/null | wc -l | tr -d ' ')
          echo "Team logo PNGs in build: $PNGS (commit web/public/team-logos/*.png to ship logos)"
          if [ -f web/dist/league-data/pickups-tenure.json ]; then
            N=$(node -e "const j=require('./web/dist/league-data/pickups-tenure.json');console.log((j.teamWaiverInTotals||[]).length)" 2>/dev/null || echo 0)
            echo "pickups-tenure.json team rows: $N"
            if [ "$N" -eq 0 ] && [ -f web/dist/league-data/transactions.json ]; then
              echo "::warning::pickups-tenure has no team totals but transactions exist — check waiver analytics build logs"
            fi
          fi
      - name: Notify push subscribers after waiver ingest
        if: steps.gate.outputs.proceed == '1'
        env:
          PUSH_API_URL: ${{ secrets.VITE_PUSH_API_URL || vars.VITE_PUSH_API_URL }}
          PUSH_INTERNAL_SECRET: ${{ secrets.PUSH_INTERNAL_SECRET }}
        run: |
          if [ -z "${PUSH_API_URL:-}" ] || [ -z "${PUSH_INTERNAL_SECRET:-}" ]; then
            echo "Push notify skipped — set VITE_PUSH_API_URL and PUSH_INTERNAL_SECRET to enable"
            exit 0
          fi
          GW=""
          if [ -f web/dist/league-data/bootstrap_draft.json ]; then
            GW="$(node -e "const j=require('./web/dist/league-data/bootstrap_draft.json');const e=(j.events?.data||[]).find(x=>x.is_current||x.is_next);process.stdout.write(String(e?.id||''))")"
          fi
          if [ -z "$GW" ]; then
            echo "Push notify skipped — could not resolve current/next GW"
            exit 0
          fi
          curl -fsS -X POST "${PUSH_API_URL%/}/internal/notify" \
            -H "Authorization: Bearer ${PUSH_INTERNAL_SECRET}" \
            -H "Content-Type: application/json" \
            -d "{\"type\":\"waiver_processed\",\"gw\":${GW}}" \
            && echo "Push notify sent for GW${GW}" \
            || echo "::warning::Push notify request failed (non-fatal)"
      - uses: actions/upload-pages-artifact@v5
        if: steps.gate.outputs.proceed == '1'
        with:
          path: web/dist

  deploy:
    if: success() && needs.build.outputs.should_deploy == '1'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5

      # Live site is on Vercel (tclot.vercel.app); Pages alone does not refresh it.
      # Optional repo/environment secret VERCEL_DEPLOY_HOOK = Deploy Hook URL from
      # Vercel → Project → Settings → Git → Deploy Hooks. When set, every successful
      # scheduled/push Pages deploy also kicks a Vercel rebuild (which re-runs ingest).
      - name: Trigger Vercel redeploy (optional)
        env:
          VERCEL_DEPLOY_HOOK: ${{ secrets.VERCEL_DEPLOY_HOOK }}
        run: |
          if [ -z "${VERCEL_DEPLOY_HOOK:-}" ]; then
            echo "No VERCEL_DEPLOY_HOOK secret — skipping Vercel trigger (Pages deploy still live)."
            exit 0
          fi
          code=$(curl -sS -o /tmp/vercel-hook.out -w '%{http_code}' -X POST "$VERCEL_DEPLOY_HOOK") || true
          echo "Vercel deploy hook HTTP $code"
          head -c 400 /tmp/vercel-hook.out 2>/dev/null || true
          echo
          case "$code" in
            200|201|202|204) echo "Vercel redeploy triggered" ;;
            *) echo "::warning::Vercel deploy hook returned HTTP $code — check the secret URL" ;;
          esac
